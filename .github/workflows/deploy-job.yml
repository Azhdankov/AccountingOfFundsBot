name: Deploy buy runner

on:
  push:
    branches: [ "master" ]

jobs:
  prepare-environment:
    runs-on: self-hosted


  deploy:
    runs-on: self-hosted
    needs: prepare-environment
    steps:
      - name: checkout repo
        uses: actions/checkout@v3
      - name: docker compose
        run: docker compose up -d